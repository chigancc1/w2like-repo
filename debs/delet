# prerequisites (Ubuntu/macOS): dpkg-dev
# macOS: brew install dpkg
# Ubuntu: sudo apt-get install dpkg-dev

git clone https://github.com/<you>/w2like-repo.git
cd w2like-repo
bash index.sh
git add -A
git commit -m "Update APT index"
git push
